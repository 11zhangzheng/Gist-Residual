extends: base.yaml
experiment_id: E09
required_fields: [production_authority, source.git_commit, inputs.observation_graph]
inputs:
  observation_graph: RESEARCH_OWNER_DECISION_REQUIRED
policies: [rule, prompt_vlm, text_adaptive]
execution:
  command: [RESEARCH_OWNER_DECISION_REQUIRED_ADAPTIVE_BASELINE_COMMAND]
  may_generate_observations: false
outputs:
  required: [results/adaptive_predictions.jsonl, results/baseline_identity_audit.json]
