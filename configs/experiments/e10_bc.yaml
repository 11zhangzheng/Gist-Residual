extends: base.yaml
experiment_id: E10
required_fields: [production_authority, source.git_commit, inputs.observation_cache, inputs.oracle_trajectories]
inputs:
  observation_cache: RESEARCH_OWNER_DECISION_REQUIRED
  oracle_trajectories: RESEARCH_OWNER_DECISION_REQUIRED
training:
  seeds: [13, 37, 73]
  cost_preferences: [0.0, 0.1, 0.3, 1.0]
  loss_weights: {action: 1.0, sufficiency: 0.3, cost_to_go: 0.1}
execution:
  command: [RESEARCH_OWNER_DECISION_REQUIRED_BC_TRAINING_COMMAND]
  may_generate_observations: false
outputs:
  required: [results/training_summary.json, results/checkpoint_manifest.json]
